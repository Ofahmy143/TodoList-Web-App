<%- include("header") -%>
    <div class="box" id="heading"> 
        <h1   > <%= day%></h1>
    </div>


    <div class="box"> 
        


            <% for(var i=0 ; i< items.length  ;  i++){ %>

                <form action="/delete" method="post">
                    <div class="item">

                        <input name="checkbox" value=<%=day %>,<%=items[i]._id %>  onChange="this.form.submit()" type="checkbox" >
                        <%if(day ==="lists"){%>
                        <a href="/lists/<%=items[i].name%>" > <%= items[i].name %> </a>

                        <%}else{%>
                            <p> <%= items[i].name %></p>
                            <%}%>
                    </div>
                </form>

           <% } %>



     
        <form class="item buttons" action="/" method="post">
            <div >
                <input type="text" name="todoItem" placeholder="New Item">
                <button type="submit" name="list" value=<%= day %>> + </button>
            </div>

            <!-- <div class="buttons">
                <button class="redirect" type="submit" name="list" value="workButton"> Work</button>
                <button class="redirect" type="submit" name="list" value="aboutButton"> about</button>
            </div> -->


        </form>
        
         
    </div>

    <%- include("footer") -%>
